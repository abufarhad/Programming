#include<stdio.h>
int main()
{
    int i=20,sum=0;        printf("the valu is\n");
    while(i<25){
        i--;
        if(i==0)break;
sum=sum+i;

        printf(" %d\n",i);}printf("sum is %d",sum);

        return 0;
}
