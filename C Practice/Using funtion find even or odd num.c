#include<stdio.h>
int fact(int E,int O)
{
    int n,result,i;
    if(n%2==0)
    return 0;
    if (n%2==1)
    return 1;
}
int main()
{
    int n,E,O;
    printf("Enter the value of n :");
    scanf("%d",&n);
    if(n%2==0)
    printf("%d is Even",n,fact(E,O));
    else
    printf("%d is 0dd",n,fact(E,O));
}
