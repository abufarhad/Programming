#include<stdio.h>
#include<conio.h>
int main(){

    char name[199],ch;
  shoefram(12,25);
  printf("Plyer 1 ,Enter your name: ");
  fgets(name[0],30,stdin);
  printf("\nPlyer 2,Enter your name: ");
  fgets(name[1],30,stdin);

  printf("\n%s,you take 0",name[0]);
  printf("\n%s,you take X",name[1]);
  getc();
  clrscr();
  do
  {
      while(!Enter)
      {
          if(khbit())
            ch=getc();
          switch(ch)
          {
              case UPARROW: box=nagetive(a[3][3],box,plyer,UPARROW);
              .
              .
              .
          }
      }
      if(quit)
        break;
      win=checkforwin(a);
  }
  while(!win)
    if(win)
  {
      .
      .
  }
   else if (quit)
   {
       .
       .
   }

          }
      }
  }

return 0;
}



