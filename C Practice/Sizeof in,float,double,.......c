#include<stdio.h>
int main()
{
    int a;float b;double c; char d;
    printf("size of a charter is :%d bytes\n",sizeof(d));
    printf("size of a integer is :%d bytes\n",sizeof(a));
    printf("size of a float is :%d bytes\n",sizeof(b));
    printf("size of a double is :%d bytes\n",sizeof(c));

    return 0;
}
