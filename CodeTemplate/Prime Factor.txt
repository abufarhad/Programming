void factor(int n)
{
    int i ,cnt=0,cn=0;
    for(i=2; i<=(int)sqrt(n); i++)
    {
        while(n % i == 0)
        {
            printf("%d ",i);
            cnt++;
            n /= i;
        }
    }
    if (n > 1) printf("%d",n);
    cn++;
    printf("\n");
    printf("%d \n",(cnt+cn));
}